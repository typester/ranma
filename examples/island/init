#!/bin/zsh

# Dynamic Island style status bar plugin for ranma.
#
# Displays a unified bar at the top-center of non-notch displays,
# mimicking the Dynamic Island appearance.
#
# Prerequisites:
#   - python3 (macOS built-in)
#   - swift (Xcode or CLT)
#   - reap (brew install typester/reap/reap)

SCRIPT_DIR="${0:A:h}"

reap --watch $PPID "$SCRIPT_DIR/plugins/island.py" &
