#!/bin/zsh

# Wi-Fi status bar plugin for ranma.
#
# Prerequisites:
#   - python3 (macOS built-in)
#   - reap (brew install typester/reap/reap)

SCRIPT_DIR="${0:A:h}"

reap --watch $PPID "$SCRIPT_DIR/plugins/wifi.py" &
